Моя структура - 1. Один бот чи два? 

✅ Рекомендовано: ОДИН бот Причини: простіше підтримка одне API, одна база легко масштабувати менше банів і плутанини 

👉 Ролі визначаються в БД, а не різними ботами. 
2. Ролі користувачів Всі користувачі — це одна таблиця users, але з роллю: buyer — покупець worker — робітник (опціонально admin) 
Користувач може: або мати одну роль або дві ролі (наприклад, адміністратор + робітник) 

3. Основні сутності (логічно) 👤 Users telegram_id role (buyer / worker) balance status (active / blocked) worker_status (offline / online / busy) 
worker_region (район або масив районів) last_activity 
📦 Products id name price active 📍 Regions id name 🧾 Orders id buyer_id product_id region_id price status created_at assigned_worker_id (nullable) 

Статуси замовлення: new — створене, ще не взяли searching_worker — шукаємо робітника assigned — робітник взяв in_progress completed canceled timeout 

4. Потік для ПОКУПЦЯ /start Вибір товару Вибір району Кнопка Купити Перевірка балансу 💸 Заморозка або списання коштів Створення замовлення зі статусом searching_worker 
Повідомлення: "Замовлення створено. Очікуйте робітника"

5. Потік для РОБІТНИКА Кнопка: На роботі / Не на роботі На роботі → worker_status = online Не на роботі → worker_status = offline 
Коли робітник online: бот відслідковує всі нові замовлення у його районі надсилає push: 🔔 Нове замовлення в районі X Товар: Y Ціна: Z [Взяти замовлення]